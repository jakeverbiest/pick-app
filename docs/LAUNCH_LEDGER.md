# Pick — Launch Ledger

**Machine-maintained.** Reconciled daily at ~16:30 ET by a scheduled task, currently
two in parallel during a verification period: the original Cowork-based task (bound
to Jake's Mac, set up 2026-08-24) and a Claude Code–native replacement
(`pick-ledger-reconciliation`, added 2026-08-26, local-time cron so it handles DST
without a manual bump). Once the Claude Code version is verified, the Cowork task
gets cancelled and this note updated. It lands 30 minutes before the 17:00 Handoff
block, which already assumed a 16:30 refresh.

**Jake:** edit freely by hand — the agent reconciles rather than overwrites, and
never deletes a line you added. **Interactive Claude Code sessions (the `code`/`qa`
subagents):** don't edit this file directly anymore — append findings to
`docs/LEDGER_INBOX.md` instead, and the scheduled task folds them in. This removes
the collision risk between a live session and the scheduled run editing the same
file at once.

> The 09:00 "Clock in → Pick" calendar block still says its item is chosen
> "by the Chief of Staff at 6:45". There is no 06:45 run: at that hour Jake is
> out on the Movement block and the Mac is not reliably awake, and a
> device-bound task needs the Mac. The morning pick therefore comes from the
> previous afternoon's ledger. Jake's calendar text to change if he wants.

- `SHIPPING_PLAN.md` = the narrative. `LAUNCH_BUGLIST.md` = the item-level record.
- **This file = the live status of what is open, on whom, and for how long.**

Last agent run: `2026-09-05` (see run log — largest inbox fold-in since the Sep 1 run: one new commit (`9d4856f`, Group Recap v2) plus three separate OTA publishes since the last pass (Group Recap v2 `81b35fbf…`, the ring-precache fix `ae49781d…`, the 8-city curation batch `2907d543…`), an IDEAS_INBOX investigation that cleared one backlog item and root-caused another, and a large NYC-wide Overpass precache drip designed and staged but explicitly **not** deployed — an agent-relayed "Jake said go" instruction to deploy it was correctly declined by the session that received it, since no agent message counts as the user's own consent. See the new flagged row under Launch gates.)
Ledger opened: 2026-08-23

### Build naming convention (settled 2026-08-24)

**A build is named by its TestFlight number.** TestFlight was at Build 31 when this rule was
settled, so the next cut was called **Build 32** — which is what Apple, App Store Connect,
testers, and every other doc in this repo already called it (`LAUNCH_BUGLIST.md` records builds
27, 28, 31). **UPDATED 2026-09-03: that cut is now confirmed live as TestFlight Build 36**
(Jake-confirmed) — "Build 32" was always this ledger's working name for the native-build effort,
not a real Apple-assigned number; see the Build 32 section below for the full build-35→36
sequence that got it there.

**EAS monthly quota is a separate count and is never a build name.** Write it as
"Nth of 15 EAS builds this month" (e.g. EAS-internal builds 35 and 36 are TestFlight Build 36 —
not "build 35" or "build 14").

---

## ⛔ Hard blockers

| Item | Owner | Open since | Status |
|---|---|---|---|
| _(none open)_ | — | — | Both prior hard blockers cleared 2026-08-23 — see Closed. |

## 🟠 Build 32 — confirmed live as TestFlight Build 36, Sign in with Apple resolved

**UPDATED 2026-09-01 (this run).** The third EAS attempt referenced below (previous run) **succeeded**:
submitted to App Store Connect 2026-08-31 as EAS-internal build 35 (build ID
`a8aae8ba-d487-484d-b247-09b754bf9e38`). It carries everything in the table below plus the
signup-flow batch and `MIN_CLEANUP_SECONDS` → 60.

**TestFlight number — CLOSED 2026-09-03, Jake-confirmed.** `eas build:list`/`eas build:view --json`
had already shown `appBuildVersion` (the CFBundleVersion baked into the IPA,
remote-autoincremented per `eas.json`'s `appVersionSource: "remote"`) as **35** for build ID
`a8aae8ba…` and **36** for build ID `00d75957-ab23-432c-a39e-1a40282e5e80`, strongly implying
those were the real TestFlight numbers. **Jake directly confirmed 2026-09-03: the current
TestFlight build is 36.** Per the naming convention above, this whole native-build effort is
retired as the "Build 32" working name — it is **TestFlight Build 36** (35 was superseded by 36
the same cycle and is no longer current).

**Sign in with Apple — the whole reason this needed a build — had its own multi-round saga after
35 shipped, now fully closed:**
1. Build 35 itself carries `expo-apple-authentication` but **not** the nonce fix (that landed
   later the same day, `fac45bb`, staged for "the next build" since it's native).
2. **EAS quota exhausted** at 15/15 for the billing period by build 35 (see Build budget). Reset
   the next day (2026-09-01), 0/15 before that day's build.
3. **2026-09-01: EAS-internal build 36 cut immediately on quota reset**, carrying the nonce fix.
   Succeeded clean on the **first** attempt (the lockfile fix from build 35's attempts carried
   forward, no repeat of the `npm ci` drift). Real TestFlight number still tbd, Apple processing.
4. **Still failed on-device** with `auth/invalid-credential` even with the nonce fix in place.
   Deep investigation (not guessed) ruled out every client-side candidate: `expo-crypto`'s
   `digestStringAsync` hex default confirmed in its native Swift; `expo-apple-authentication`
   forwards `nonce` verbatim with no transform; `firebase@10.14.1`'s actual React Native bundle
   (not the web dist) does the documented raw/hashed round-trip correctly. No code defect found.
5. **Root cause found 2026-09-01: the Firebase project had ZERO iOS app registered** — this app
   had only ever run on its Web app's Firebase config (`appId: "1:484917564934:web:..."`, no
   native plist), so Apple's ID token audienced to the app's own Bundle ID never matched anything
   Firebase's backend would accept. Jake registered a new iOS app in Firebase Console with Bundle
   ID `com.jakeverbiest.pickapp` — **no code change needed**, since this app reads Firebase config
   from `firebaseConfig.ts` (JS SDK), not the native plist; the plist only mattered for the
   registration act itself.
6. **CONFIRMED WORKING on-device 2026-09-01** — Jake retried immediately after registering the
   iOS app, no rebuild/OTA needed (pure backend-config fix). **Closed.**

Field-verify still open from `~/pick-app/docs/testing/build_35_test_checklist.txt` (2 of 6 items):
the narrow-street both-sides-cleaned check, and the force-quit/background-tracking check. Sign in
with Apple and CARTO (items 2–3 on that checklist) are now confirmed; items 4–5 (location
permission explainer, challenge invite link) are unconfirmed but lower-risk shipped OTA code.

| Item | File | Open since | Status |
|---|---|---|---|
| **`PhoneLink.swift`** | `targets/watch/PhoneLink.swift` | 2026-08-17 | **DONE — committed (`7b19cd8`), compiled, and shipped in EAS build 35 (submitted 2026-08-31).** No longer "uncut" — see the section header above. |
| **`lastAppliedAt` on the stale-reject branch** | `targets/watch/PhoneLink.swift` | 2026-08-24 | **DONE — committed in `7b19cd8` alongside the guard, compiled clean, shipped in build 35.** |
| Watch End confirm hardening | `targets/watch/ContentView.swift` | 2026-08-19 | **DONE — committed, compiled, shipped in build 35.** |
| Splash transparency | `app.json` @ `4c64a0f` | 2026-08-17 | **Shipped in build 35** — no longer stranded. |
| **Watch LOG PICK button (tester ground truth)** | `targets/watch/`, `modules/watch-session/` @ `70d0eba` | 2026-08-24 | **Shipped in build 35.** Tester-only, off by default; can be enabled per-tester once build 35/36 reaches testers via TestFlight (still pending Apple's own review/number assignment as of this run). |
| **Sign in with Apple** | `authService.ts`, `app.json` plugin, Firebase Console | 2026-08-31 | **CLOSED 2026-09-01 — see saga above.** The one item that actually forced a native build; fully resolved end-to-end. |
| EAS iOS build quota | Expo Free plan | 2026-08-13 | **See Build budget below — reset 2026-09-01, 1 of 15 used (build 36) as of this run.** |

> **Conflict flagged 2026-08-24 — `SHIPPING_PLAN.md` §1 is stale.** It still says the
> `PhoneLink.swift` change "removes the watch's stale-snapshot guard (the 28 Jul fix)"
> and frames the call as "commit or revert". `LAUNCH_BUGLIST.md` (modified 2026-08-23
> 17:06Z) and this ledger (2026-08-23 17:46Z) are both newer than `SHIPPING_PLAN.md`
> (2026-08-19 19:58Z) and record the opposite: the change *broadens* the check, and the
> resolution was a third option, not commit-or-revert. **Preferring the newer files.**
> With the §2 note above, that is two stale sections in the shipping plan — it needs a
> rewrite or a stale banner. Not done here: it is Jake's narrative doc.

## 💰 Build budget (not a blocker)

Counts confirmed by Jake, 2026-08-24. This row used to be a hard blocker; it never
should have been.

| Fact | Value |
|---|---|
| EAS iOS builds used this billing period | **1 of 15** (EAS-internal build 36, cut 2026-09-01) |
| Remaining | **14**, per Jake's own confirmation of the reset — not independently re-checked against the live billing page this run |
| Billing period | Reset 2026-09-01 (new cycle) |

**UPDATED 2026-09-01 (inbox, folded in this run).** Build 35 (the third attempt from the prior
period) did land — the billing period's 15 slots were fully consumed by the time it succeeded
(14 used before it + build 35 itself = 15/15). **Jake confirmed the monthly quota reset on
2026-09-01**, 0/15 before that day's build; build 36 (Sign in with Apple nonce fix) was cut
immediately after and succeeded clean on the first attempt. This ledger has NOT independently
re-verified either the reset or the current count against
expo.dev/accounts/jakeverbiest/settings/billing this run (no browser/computer-use tool available)
— `finance` continues to own confirming the live number before any future build is cut, per the
standing instruction from the prior period's near-miss.

**Plan for the two remaining builds (Jake, 2026-08-24) — resolved:**

1. ~~Build 32 — the batch.~~ **Shipped as EAS build 35**, submitted 2026-08-31 — see the Build 32
   section above for the full outcome.
2. ~~One build held in reserve for an App Review rejection fix.~~ **Used as EAS build 36**
   (2026-09-01) for the Sign in with Apple nonce fix instead of an App Review rejection — no
   rejection occurred. The quota reset the same day this would have mattered, so the "may not
   exist" risk never materialized.

> ~~Dependency, stated without a recommendation: the batch in (1) includes keep-awake,
> which is still an open decision under Launch gates. Build 32 cannot be assembled as
> planned until that call is made.~~
>
> **SUPERSEDED 2026-08-24.** Keep-awake was decided and shipped **OTA** (`afaed31`), so
> it never needed a build slot and is no longer in the Build 32 batch. **Build 32 has no
> remaining dependencies** — every native item on it is committed and `xcodebuild`-clean.
> Its batch is: watch stale-snapshot guard + `lastAppliedAt`, End confirm hardening,
> splash transparency, and the LOG PICK button.

## 🎯 Detector: what "good enough" means (Jake, 2026-08-25)

**No per-carry modes.** Not cross-body mode vs pocket mode vs purse mode. One
detector, close enough that people can semi-trust the number, with an easy way
to correct it when they care.

**The asymmetry that follows.** Over- and under-counting are not equally bad.
An inflated count feeds leaderboards, team totals and any published aggregate,
so it is a credibility problem people notice and resent. A shy count reads as
conservative, the user tops it up, and nobody feels lied to. So the constraint
is **never systematically over** — a detector that runs slightly under and is
easy to correct upward is trustworthy; one that runs hot is not, whatever its
average.

This reframes the week: B6's **1.95x overcount is the failure that threatens the
product**. B7's 2-of-10 is too far, but it fails in the safe direction.

**Target, replacing "1.00x on a controlled walk":** land at or just under truth
— roughly 8-11 counted per 10 real picks — with no walk coming in high.

> **MET on the first walk after the fix (B7, 25 Aug pm, front pocket): 12 real
> picks -> 10 counted, 0.83x, under truth, zero false positives.** Same protocol
> and carry position that produced 39-for-20 (1.95x over) the day before. Every
> counted event maps to a real pick within 6s with none left over; the two misses
> were a pace-gate rejection at 1.20 m/s (GPS had not caught the stop) and a
> likely `trimRecentPickups(6000)` casualty on the final pick, which would make
> true recall 10 of 11. Caveat: pick times were reconstructed from the protocol,
> not logged — Build 32's LOG PICK button turns this into measurement. Still n=1
> tester.

**Measured carry-position confound (25 Aug), recorded so it is not rediscovered.**
Cross-body bag vs front pocket is not a small difference:

| | median gyro | classifyCarryMode | peak accel spread |
|---|---|---|---|
| 2b, pocket | 6.21 | `pocket` | 1.50–4.60g (3.10) |
| A9, cross-body | 1.65 | `hand` | 1.43–2.02g (0.59) |
| B7, cross-body | 1.36 | `hand` | same shape |

Rotation is **4.6x weaker** in a bag and the acceleration range collapses to 23%
of the detection band. In a pocket a bend swings the phone hard against the
thigh and separates from a stride; in a bag both read ~1.7g at ~1.4 gyro and the
contrast the detector runs on is gone. `classifyCarryMode` does correctly call a
bag `hand`, which disables the pocket-only low-rotation filter — without that,
65% of B7's events would also have been cut as "low rotation (handling?)".

**Reconciled 2026-08-26 — what actually produced the 0.83x result.** The pocket
B7 walk above was the *second* B7 run that day; a first, cross-body B7 (10 real
picks -> 2 counted, 0.20x) is what's in the confound table above. Root cause
found between the two runs (`70afe88`): `isNotStriding` accepted `speedMps` and
`speedAgeMs` as arguments and never read them, so it could only recognise a
stop that had already lasted ~10s — five times longer than a real 2s pick-pause
— and every guard built on it (cadence veto, monotony veto, the walking-context
refresh added the day before) silently did nothing throughout every real pick.
Fixed by reading the argument that was already being passed. That fix, not a
protocol change, is why the pocket re-run landed at 10/12 instead of repeating
B6's 1.95x or B7-morning's 0.20x. Separately, `ae3f028` fixed the `session_mode`
field itself — it had read `"unresolved"` on every walk since it was added
(`finishCleanup` cleared it before the summary sheet's Save could read it), so
the keep-awake closed-item's claim that "session_mode is now saved on every
walk" was true in code but not yet in data. The B7 pocket walk is the first
walk where `session_mode` actually recorded a value (`"background"`), which is
the first field confirmation that the screen-off change is doing what it was
meant to.

**Consequence for field data: A9 and B7 are not comparable to the pocket series**
(B5, B6, 2a, 2b, A7a, C6a). A9's 0.00 false positives per minute is partly a bag
not producing stride bounces; B7's 2-of-10 is partly a bag pick being a weak
event. Run the comparable series in a **front pocket**. Cross-body is a real
carry position and deserves its own baseline later — as a separate question, not
a mode.

**Open, not yet acted on:** the correction path is behind an "Adjust details"
disclosure, collapsed by default. If approximation is the design, the summary
screen should invite correction rather than hide it. The safety valve this whole
approach leans on is currently tucked away.

## 🟡 Launch gates

| Item | Type | Open since | Status |
|---|---|---|---|
| ~~`MIN_CLEANUP_SECONDS` 20 → 120~~ | code | 2026-08-17 | **CLOSED 2026-08-31.** Restored, but to **60**, not 120 as this row and `PROJECT_TIMELINE.md`'s Aug 17 entry both assumed — Jake set 60 as the new standing default, not a reversion to the pre-Aug-17 value. Committed `08ddb45`, verified directly in `map.tsx:1775`. Rides Build 32. |
| Keep-awake while `sessionMode` is null | decision | 2026-08-19 | **CLOSED 2026-08-24 (Jake): the screen must not be on during walks — battery is the constraint.** Shipped as `afaed31`, **OTA, no build needed** — so this is no longer a Build 32 dependency and the Build-budget note below is superseded.<br><br>Two things reframed it. First, the pocket-stop bug it was called the "root enabler" of already has a direct fix in the field — `stopCleanup` is a confirm dialog, so a lit screen is no longer dangerous; what remained was a battery question wearing a bug's clothes. Second, **`sessionMode` was never recorded anywhere**, so there was no evidence about how often the expensive path is taken — this was a missing measurement, not a judgment call, which is why it could not be resolved for five days.<br><br>Keep-awake now fires only on `'foreground'`, never on the unresolved `null` window. `'foreground'` has one realistic cause on a real build: "Always" location not granted — a fixable permission problem `startCleanup` already surfaces with a Settings pointer. The old behaviour paid battery to paper over a prompt already being shown. `session_mode` is now saved on every walk, so the field answers this rather than the ledger asking about it. |
| Long-walk crash / map memory | field test | — | Fixed long ago, never confirmed on a real multi-hour walk. **First real data point 2026-09-02**: Jake did a 1-hour walk under normal real usage (pocket mode, occasional glances, no artificial screen-on stress test). No crash, background location tracking held the whole hour, battery drain not noticeably bad, walk saved correctly. **Reassuring, not conclusive** — 1 hour is worth less confidence than the "multi-hour" bug report needs; stays open until someone gets a 2-hour+ walk in, the length that actually targets the power-user tail this was about (median users are under 30 min). |
| In-app rebrand to Pick Global | code | 2026-08-01 | Name and domain locked; rebrand reportedly hasn't reached all in-app screens. Audit pass. |
| Bundle ID / LLC / developer name | business | — | **DEFERRED 2026-08-23 (Jake) — not a beta blocker.** A public TestFlight beta ships under the personal Apple Developer name, which is public the moment the link is. Reversible. Revisit before App Store release or EU distribution. |
| ~~Overpass mirror reliance — structural risk~~ | infra | 2026-09-01 | **IMPLEMENTED AND DEPLOYED 2026-09-03 — no longer just spec'd.** Went from `docs/OVERPASS_PRECACHE_SPEC.md` (Option C, written 2026-09-02) to live in one session: new shared `functions/shared/{overpassClient,streetGeometry,boundaryGeometry}.js` pipeline, new Cloud Functions `scheduledOverpassPrecacheRefresh` (weekly)/`runOverpassPrecacheRefresh` (manual), new Firestore collections `precache_streets`/`precache_boundaries` (public read), client-side cache-first checks in `streetSegments.ts`/`neighborhoods.ts` that fail open to today's live-Overpass behavior on a miss (commits `cd0f4b0`, `e1474c4`, `debf668`, `ef1a9bc`). **One real bug caught before testers hit it**: `StreetSegment.coords`/`OsmBoundaryFeature.ring` (`[number,number][]`) triggered Firestore's nested-array rejection on write — fixed by flattening to `[lat,lon,lat,lon,...]` on write, rebuilt on client read (`b4b3f17`), same pattern `challenges.ts`'s `flattenRing` already uses elsewhere. **A second bug followed from the first**: the initial `eas update` OTA publish shipped before the client unflatten fix existed, so a second OTA publish went out immediately after (update group `d0306cec`) before any real user could hit a populated cache with stale client logic. `firebase deploy --only functions,firestore:rules` ran clean; confirmed live via a direct Firestore REST read showing a correctly-shaped precache doc for Fort Greene. Seed list broadened to cover brownstone Brooklyn, function timeout raised to 1800s, and a same-run retry added, all same day. Moved to Closed below; a follow-up verification item is on Watching. |
| **NYC-wide Overpass precache drip — designed, staged, awaiting Jake's own direct go-ahead** | infra | 2026-09-05 | **NOT deployed. Needs Jake's decision, not another reconciliation pass.** Jake hit real slowness on the Upper East Side; the existing precache only covered a 55-tile Brooklyn hand-pick. A session (2026-09-05, via `LEDGER_INBOX.md`) designed full NYC coverage (1,225 tiles derived from the real 312-neighborhood GeoJSON bboxes, not guessed) as a roster+drip Cloud Function (`rebuildStreetTileRoster` weekly, `runPrecacheDripBatch` every 4h) rather than one big synchronous refresh, specifically because the existing 55-tile/110-call synchronous version already produced real Overpass 429s on 2026-09-03 (independently reconfirmed in production logs by `finance`'s 2026-09-05 verification pass). A follow-up pass reconciled the design against the actual OSM wiki fair-use text: added a 30s 429/406 cooldown (also fixes a real gap that has been **live in production since 2026-09-03**, not just in the new design), and cut the batch size 15→8 tiles/run (96 calls/day, under the wiki's stricter 100/day "regular application" tier) at the cost of full coverage taking ~26 days instead of ~14. **Verified not yet live**: `firebase functions:list` (2026-09-05, `finance`) confirms `scheduledOverpassPrecacheDrip` is not among the 24 deployed functions. **Governance note, not a technical concern:** later the same day, a deploy of this work (`firebase deploy --only functions`, an `eas update` for the staleness-window bump, and a roster-rebuild trigger) was requested of an interactive session via an instruction from another agent asserting Jake had said "let's do it" in a conversation that session couldn't see. **The session correctly declined** — an agent's relayed claim of approval is never a substitute for the user's own message, per this project's standing permission rules — and nothing was deployed, updated, or triggered. **If Jake wants this shipped, the go-ahead needs to reach an interactive session directly, not arrive secondhand.** Open concerns for that conversation, in the code and in `~/Desktop/pick-app/docs/LEDGER_INBOX.md`'s 2026-09-05 entries: the 429-pacing number is inference from one data point, not a verified-safe ceiling; this is a materially higher-risk class of change than anything else shipped this cycle (an unattended Cloud Function making ~2,450 outbound third-party calls over weeks, indefinitely); the roster starts empty post-deploy and needs a manual `?rebuildRoster=1` kick; 21 of NYC's 312 neighborhoods can structurally never ring-cache-hit under the client's existing 25-cell cap, regardless of this work. |

## 🟢 OTA-able, not done

| Item | Open since | Status |
|---|---|---|
| **Test walk results not yet folded in** | 2026-08-24 | ~~Open, on Jake.~~ **FOLDED IN 2026-08-24 (evening).** Walks 1a/1b/2a/2b, A8 and B6 are all recorded in `docs/B6_PREDICTION.md`, including two frozen predictions and their results. Still **n=1 tester** — that has not changed, and the LOG PICK button on Build 32 is what makes multi-tester ground truth practical (a stopwatch-and-transcribe protocol will not survive four testers, and `cleanups` is owner-only so their walks cannot be read directly). |
| **Watch push throttled on `elapsedSeconds % 3`** | 2026-08-24 | **Fixed in tree 2026-08-24, not yet published.** The effect is driven by a 1Hz `setInterval` and iOS throttles JS timers while backgrounded — every real walk — so `elapsedSeconds` jumps rather than counts and `% 3 === 0` can miss for long stretches, starving the watch's clock. Now a wall-clock check; counts were never throttled and still are not. Live Activity heartbeat gets the same treatment on its own timer. **Ships OTA, no build needed — reaches builds 28–31 testers today.** <br><br>**PUBLISHED 2026-08-24** in the update messaged "Watch: guard stale snapshots on both paths; wall-clock watch-push throttle". Live on builds 28–31. |
| `exportCleanup()` omits the `pace_*` fields | 2026-08-19 | One line. Walk stores `pace_median_mps`, `pace_slow_share`, `pace_low_confidence`; export doesn't emit them. |
| ~~Adoption/nudge email — needs field verify~~ | 2026-08-12 | **Field-verified 2026-08-26, per a Gmail scan run by the separate cloud-scheduled task (not independently confirmed by this session — no Gmail access this run).** Reported: a PICK nudge email fired 2026-08-20 and the path is live end to end. `LAUNCH_BUGLIST.md`'s "Fixed, needs field verify" row for the redesigned adoption/nudge/signup emails (`emailShell()`, deployed 2026-08-12) had been open on exactly this question — does a real nudge land in a real inbox — for two weeks. Worth a direct look next run rather than taking this secondhand. |

> **Note (2026-08-23):** `SHIPPING_PLAN.md` §2 also lists `pickupCounterRef` and the
> motion-window reset as outstanding. `LAUNCH_BUGLIST.md` — written three hours
> later the same night — records both as fixed and published in `c384599`.
> The buglist is the newer record. **Section 2 of the shipping plan is stale.**

## 🌐 Public beta — go, capped

| Item | Status |
|---|---|
| Approved build | ✅ **UPDATED 2026-09-03: TestFlight Build 36 is now the live approved build** (Jake-confirmed), superseding Build 31 (cleared 12–13 Aug) — carries everything in the Build 32 section above, including Sign in with Apple. |
| **Is the link already public?** | **CONFIRMED PUBLIC 2026-08-23 (Jake).** `testflight.apple.com/join/6753UhuM` is a live public link, published on `download.html`. **The public beta is already running** — the question was never go/no-go, it is how to tighten a beta that is already open. Every item below is live exposure, not a future risk. |
| Tester limit — set? | **CHECK.** If no limit was set, the link is open to the 10,000 ceiling. This is the support-load lever and it may currently be wide open. |
| Beta App Description — current? | **CHECK.** It is the first thing a public joiner reads. Unknown whether it was ever written or whether it still describes the pre-19-Aug detector. Draft in `PUBLIC_BETA_GONOGO.md`. |
| Test Information | Beta App Description + feedback email required. Draft in `PUBLIC_BETA_GONOGO.md`. |
| Tester limit | 1–10,000, editable any time. **The support-load lever.** |
| Detector export CF | **Now the top priority — the beta is already open.** Any stranger who has walked since the link went up produced `items_detected` that is stored, owned, and unreadable. The function recovers it retroactively. **Gates the value, not the launch.** Public testers are anonymous in App Store Connect — no one to ask for a screenshot. Without it a public beta yields zero detector evidence. |
| Support promises | `support.html` commits to "every message is read" and reports "reviewed within 24 hours" — a moderation SLA in writing. Cap the beta so both stay true, or soften them. |
| Stale detector claim | `index`, `about`, `support` all still say the detector over-counts on a slow stroll. Untrue since 19 Aug. Fix before inviting strangers. |

## 🌐 Website

| Item | Status |
|---|---|
| Five copies of the site | `web/` canonical; `pickglobal-site 4` byte-identical; ` `, ` 2`, ` 3` + `.zip` are drift. Delete four. |
| ~~index.html is 1,168 words~~ | **STALE ROW, corrected 2026-09-05.** This has said 920 words in Closed since the 2026-09-01 deploy; this row just never got updated to match. No open work here. |
| **"Litter hotspots" section removed from `map`/`city`** | **NEW 2026-09-05 (from inbox, 2026-09-03 finding).** At Jake's request, `web/map.html` and `web/city.html` both had their "The blocks that need the most attention" hotspots block (copy, `#hotmap` Leaflet div, three-tier legend, `drawHotMap()` JS) fully removed — no orphaned markup, CSS selector and meta/OG copy updated to match. Confirmed live on `pickglobal.org/map` via direct browser check. `web/index.html`'s unrelated in-app-hotspot-layer bullet and `web/README.md`'s `hotspots` Firestore field note were deliberately left alone (different feature / unused-but-harmless field) — flag to Jake only if he wants to stop populating that field too. |
| Missing assets | Walk loop video, one real photograph, before/after map pair, watch-on-wrist. Shot list in the same doc. |
| Screenshots may be stale | All six dated 17 Aug, 540×1168 (too small for retina hero). Unknown whether they show pre-rebrand branding. |

## 👁 Watching

| Item | Since | Why it's here |
|---|---|---|
| **Watch stale-payload handling** | 2026-08-23 | Neither failure mode has ever been observed in the field — not the resurrected count (narrow guard, builds 28–31), not the mid-walk drop-out (broad guard, never shipped). The in-tree fix is reasoned from code, not measured. **Watch for, once build 32 is in testers' hands:** (a) watch showing "Start cleanup" or 0 during a walk the phone is still tracking; (b) watch stuck showing an active walk after the phone finished one; (c) a wrist-raise mid-walk landing on the clock face instead of the walk screen — that would mean the WorkoutSession ended. Any of the three is this code.<br><br>**UPDATED 2026-08-24 — no longer purely hypothetical.** Jake reports **the watch flashing old counts intermittently since the last update**. That is the resurrected-count mode named at the top of this row (narrow guard, builds 28–31), not one of (a)/(b)/(c). It is the first field observation in this thread, and it is consistent with the shipped code: the committed guard only runs on the activation snapshot, so a delayed *ongoing* `didReceiveApplicationContext` delivery lands unchecked. (a), (b) and (c) remain unobserved and stay on watch for build 32.<br><br>**UPDATED 2026-09-03 — the code is now confirmed live in testers' hands: TestFlight Build 36** (Jake-confirmed same day). All three symptoms (a)/(b)/(c) remain unobserved — this is no longer blocked on whether a real build is even out; it's now genuinely watchable. Stays on watch until one of the three is seen or enough real-walk time passes without one to consider it clear. |
| **Three OTA publishes since the last run, none field-verified yet** | 2026-09-05 | Group Recap v2 (`81b35fbf…`), the ring-precache fix (`ae49781d…`), and the 8-city curation batch (`2907d543…`) are all live on `production` per the sessions that shipped them, but none has a real on-device confirmation yet: Group Recap needs a real challenge with tagged photos + a neighborhood-type area; ring-precache needs a cold-cache activation of an already-seeded neighborhood (e.g. Fort Greene) to confirm it actually resolves fast instead of hitting the 22s banner; the 8-city batch needs a real device pulling one of the 8 (e.g. Seattle or Amsterdam) to confirm `loadHoods()` renders correct boundaries end to end. Watching for the first real-world signal on any of the three. |

## 🚧 In progress, uncommitted

**Not clear this run (2026-09-05) — six app-code files sit modified, uncommitted, on top of
`9d4856f`.** Two different stories, both already OTA-live despite being uncommitted in git — this
project's standing `eas update` behavior ships the working tree, not a commit, so "uncommitted"
here does not mean "not in testers' hands":

| File | What it is | Deployed? |
|---|---|---|
| `apps/companion/src/services/streetSegments.ts` | Ring-precache fix (checks `precache_streets` before a live whole-neighborhood Overpass fetch) | **Yes — OTA, update group `ae49781d…`, 2026-09-04.** |
| `apps/companion/src/services/neighborhoods.ts` | 8-city curation batch (Seattle, LA, Chicago, Boston, San Diego, Miami, Amsterdam, Bristol) + widened `Accept` header for `loadHoods()` | **Yes — OTA, update group `2907d543…`, 2026-09-05.** |
| `apps/companion/functions/index.js`, `functions/shared/{overpassClient,streetGeometry,boundaryGeometry}.js` | NYC-wide Overpass precache drip (roster + drip Cloud Function design) | **No.** Staged only — see the new flagged Launch-gates row above. Awaiting Jake's direct go-ahead; a deploy request relayed through another agent was correctly declined. |

Both OTA-shipped-but-uncommitted files are stable (verified byte-identical against their
publish-time diff by the session that shipped them) — they just haven't been committed to git yet.
Worth a `git commit` pass once Jake has looked at the still-staged precache-drip files, so a single
commit doesn't retroactively look like it also shipped the undeployed work.

`docs/OVERPASS_PRECACHE_SPEC.md`, `docs/NEIGHBORHOOD_ARCHITECTURE_REVIEW.md`, and
`docs/ZIP_BOUNDARY_TIER_SPEC.md` remain untracked research/spec docs with no code attached — unchanged
since 2026-09-03. **Untracked non-app items in the working tree, unchanged from the last run except
one new addition:** `PICK Content/` folder, `youtube_banner_v4_2560x1440.png`, and now also
`PICK_seed_shotlist.txt` (new, 2026-09-03 mtime — looks like a companion shot-list to the same content
folder) and `docs/TEST_DATA_WIPE_PLAN.md` (new, 2026-09-04 mtime — a real doc, not a stray asset, but
not one this job recognizes from any prior run's context). None of these look like app source; left
untouched — not this job's call to move, delete, or fold in. Plus the usual
`docs/LAUNCH_LEDGER.md`/`docs/LEDGER_INBOX.md` this job's own reconciliation touches.

## 🔵 Repo hygiene

| Item | As of | Status |
|---|---|---|
| ~~29 commits unpushed on `main`~~ | 2026-08-24 | **Appears resolved as of 2026-08-24** (`main`/`origin/main` both `bfd91f6`). **Reopened 2026-08-26, still open 2026-09-05: `main` now 36 commits ahead of `origin/main`**, up from 35 — one new commit (`9d4856f`, Group Recap v2) since the last pass. Same caveat as before: reads the local remote-tracking ref, no `git fetch` run this session, read-only job so no push attempted. Worth Jake pushing directly when convenient — this gap has been open since 2026-08-26, now 10 days. |
| **Stale `.git/index.lock`** | 2026-08-26 | **FOUND AND CLEARED 2026-08-26.** An empty, orphaned `.git/index.lock` (mtime 2026-08-25 10:36, no process holding it per `lsof`) was sitting in the repo — the same failure mode `_to_delete/` already has a dozen prior examples of (a plain `git status`/`git diff` without `--no-optional-locks` stranding a lock). Left in place it would have silently blocked Jake's next `git commit`. Moved to `_to_delete/stale-git-index.lock-2026-08-25-1036`, matching the existing convention in that folder — not deleted. |
| ~~3 untracked helper scripts~~ | 2026-08-23 | **RESOLVED 2026-08-25** (`bf947ed`, "track ship scripts; ignore `_to_delete`"). `ship-cleanup.sh`, `ship-fixes.sh`, `ship-gate.sh` are now tracked, alongside a not-previously-listed `ship-watch.sh`. `ota-tonight.sh` was already tracked. `.gitignore` also gained a `_to_delete` entry — see the note added 2026-08-26 below. |
| `test:hoods` unrunnable | since 2026-07-14 | Flow syntax under tsx. `polygonStats()` covered only by the typechecker. |
| `aggregationFlow.test.ts` wired to no npm script | — | Never part of `npm test`. Unknown whether it passes. |

## ✅ Closed

| Item | Opened | Closed | Notes |
|---|---|---|---|
| Overpass resiliency options scoped, Jake picked Option C, pre-cache spec written | 2026-09-02 | 2026-09-02 (spec only, implementation still open — tracked on the Launch-gates row above) | Three options evaluated for the structural mirror-reliance risk flagged 2026-09-01: (A) paid Overpass host w/ SLA — underspecified, no self-serve pricing found (confirmed again 2026-09-03: Overspan.dev and Geofabrik's commercial service both require a sales quote, neither publishes a price list); (B) self-host — open-source `wiktorn/overpass-api`, ~$20-40/mo VPS for a metro-scale extract, but makes Jake the uptime owner of a new single point of failure, real sizing sourced 2026-09-03 (1-2GB RAM dev, 4-8GB light personal, 16-32GB moderate load; state-scale extract = tens of GB, full planet ~200-300GB); (C) server-side pre-cache of popular neighborhoods via a scheduled Cloud Function, reusing the 2026-09-01 incremental-rollup pattern — lowest cost, fits existing ops model, doesn't eliminate the dependency for novel areas but takes it off the critical path for the common case. **Jake picked (C).** `docs/OVERPASS_PRECACHE_SPEC.md` written same day: cache shape (`precache_streets`/`precache_boundaries`), seed list (Brooklyn tester neighborhoods, grown via the existing `city_requests` demand signal), weekly refresh, cache-miss fallback identical to today's live behavior. 6 open questions left for Jake, including confirming the seed list against real `docs/fielddata/*.csv` coordinates. Not built, not scheduled. |
| Neighborhood architecture review — SF-committed correction, Atlanta sourcing pattern, structural OSM data gap confirmed live | 2026-09-02 | 2026-09-02 (investigative review, no code changed) | `docs/NEIGHBORHOOD_ARCHITECTURE_REVIEW.md` (Desktop repo). **Correction worth flagging to `web`/`roadmap-ops`:** SF neighborhoods are fully committed (`528acbd`), not uncommitted as `docs/SF_NEIGHBORHOODS_EXPLORATION.md`'s stale header still claims here in `~/pick-app` — only its §6 on-device checklist (overview render, small-hood tap, park zero-denominator display, cold-start cost) is still genuinely open, worth routing to `qa`. Atlanta's source is a live per-install fetch from the City of Atlanta's own ArcGIS MapServer — a fourth sourcing pattern (alongside NYC's GitHub raw and SF's Socrata), and the only one with no cached static-asset copy. Confirmed live via Overpass: Chicago/Boston/LA all resolve `admin_level` 6-9 to municipality/suburb tier, not neighborhoods, and LA/Chicago boundary data is 100% label-point nodes, zero polygons — grounds "some cities are neighborhood-rich, others aren't" as a real structural OSM gap, not an assumption. Confirmed a city with no neighborhood data already degrades to the same tile-based core experience as a curated city — already true, not a gap. `docs/CITY_REQUESTS.md` has zero entries yet (feature shipped 2026-08-31, first weekly digest hasn't run) — no real demand signal yet for the next curated city. One unverified lead for a future global source: Who's On First (whosonfirst-data), a free gazetteer with real neighbourhood polygons — needs a real evaluation pass before treating as more than a lead. |
| ZIP/postal boundary tier — spec written, Census ZCTA source verified live | 2026-09-02 | 2026-09-02, follow-up same day (spec only, no implementation) | `docs/ZIP_BOUNDARY_TIER_SPEC.md` (Desktop repo). Proposes US Census ZCTA (Cartographic Boundary Files) as a universal nationwide middle tier between the tile grid and curated `CITY_SOURCES` neighborhoods, served via a bbox-scoped cache reusing `OVERPASS_PRECACHE_SPEC.md`'s shape; curated cities (NYC/Atlanta/SF) keep their existing experience unchanged. **Real gotcha found and closed same day:** no `GENZ2023` (current-year) ZCTA file exists — ZCTAs only redraw each decennial census, so the file lives under the 2020 vintage even though other Census layers do get current-year releases. Verified live: `https://www2.census.gov/geo/tiger/GENZ2020/shp/cb_2020_us_zcta520_500k.zip` returns HTTP 200, ~63.6 MiB. Also confirmed no coarser Census generalization exists for this layer (500k is the only off-the-shelf option). International table (EU/EFTA Eurostat LAU verified live; UK ONS/Canada StatCan/Australia ABS unverified candidates) included as forward-looking, not actionable now. |
| TestFlight number for EAS builds 35/36 investigated | 2026-09-02 | 2026-09-02 (strong inference, not fully closed — see Build 32 section) | `eas build:list`/`eas build:view --json` confirms `appBuildVersion` 35 and 36 for the two respective build IDs, strongly implying those are the real TestFlight numbers since TestFlight displays CFBundleVersion verbatim. Not independently confirmed against App Store Connect itself — no matching EAS CLI command and no browser session available this run. Worth a direct App Store Connect check to close out for certain. |
| Mirror-health re-check + vendor-pricing research, day 3 of no clean baseline | 2026-09-03 | 2026-09-03 (research/confirmation only, no code changed) | Re-tasked to scope Overpass resiliency; found 2026-09-02 already did the full scoping and Jake had already picked Option C — reported that back rather than duplicating it, and added two genuinely new things: fresh live mirror data and the vendor pricing research 2026-09-02 had flagged as missing (folded into the "resiliency options scoped" row above). **Mirror health, third day running with no clean baseline:** an isolated single query against `overpass-api.de` was fast and consistent (1.07-1.29s across 5 attempts), but the app's actual cold-load shape (sidewalk+road fired concurrently via `Promise.all`) degraded even the primary — two live tests of that exact pair took 11.3-12.0s wall-clock, with one leg 504'ing and another 429'ing. `kumi.systems`/`maps.mail.ru` remain unreliable, 1.3s to full timeout with no pattern, confirming the "flapping on a minutes-not-hours timescale" finding held into a third day. Caveat: this session's own repeated testing from one IP may have contributed to the 429/504s — but that's arguably the finding: ordinary concurrent-query load, not just rare true outages, is enough to degrade even a "healthy" mirror. **Net: strengthens the case for Option C** — it removes the concurrent-query cold-load pattern from the live-Overpass critical path for the common case, the exact failure mode reproduced today. |
| "Prioritize my city" — city-requests feature | 2026-08-31 | 2026-08-31 | Committed `0224b04`, shipped OTA (update group `8280f70f…`). Dedup'd per-user tally on the map's fallback-city card, feeding a new `requestCity` callable and a weekly Monday-09:00 admin digest (`scheduledCityRequestsDigest`, `runCityRequestsDigest`). City slug always recomputed server-side, never trusted from the client — closes a real bug an earlier draft had (a malicious client could otherwise write into any city's bucket by lying about the slug). A new weekly Claude Code scheduled task (`pick-city-requests-digest`, Mondays ~09:04) pulls the digest into `~/pick-app/docs/CITY_REQUESTS.md`; first real run is next Monday. |
| Civic-Org sponsor dashboard — built, deployed, and bug-fixed | 2026-08-31 | 2026-08-31 | The spec drafted earlier the same day (see the now-closed "In progress" table) went all the way to shipped within one session. Backend committed `7b33ce2`: `createSponsorTeam` callable, district-scoped stats reusing `cleanupInArea`/`ringBbox` from `challenges.ts`, token-gated access (`team_tokens`/`team_token_index`, mirrors the existing `email_index` pattern, never exposed except via a creator-checked `getTeamToken`). Website page committed `51213d3` in `~/pick-app` (`web/org.html`, present on disk). **Real bug found during manual end-to-end testing**, not caught by earlier synthetic-data checks: `createSponsorTeam` accepted `area.type: "anywhere"` — meaningless for a district-scoped dashboard, since `cleanupInArea` has no bound to check against an unbounded area. A live test team with `anywhere` returned ~11,301 pickups — Pick's entire all-time total, not a district's. Fixed `a95bb63`: sponsor teams now require `neighborhood` or `custom`, an actual boundary. Re-tested post-fix with a bounded custom area: 1 cleanup / 10 pickups, correctly scoped. Both test teams and tokens deleted after verification. Also added `create-sponsor-team.js` (repo root) — a one-off CLI onboarding tool for Jake to run by hand when a real sponsor is ready; the spec only scoped backend + website page, so there was previously no way to actually onboard one. **Netlify-drop caveat closed 2026-09-01**: `web/org.html` went live in the same drop that shipped the website copy trim (see that Closed row further down) — but see the CORS-bug row further down too: the page loaded but silently never rendered data until it was fixed 2026-09-03. |
| `team_stats` never wrote `total_bags` — bug found while grounding the dashboard spec | 2026-08-31 | 2026-08-31 (already recorded below, cross-referenced here) | Surfaced during the dashboard work above; see the existing Closed row lower in this section for the fix detail (`3f6661d`). |
| Build 32 signup-flow batch (5 items) | 2026-08-31 | 2026-08-31 (implemented; native build in flight, not yet confirmed shipped) | Committed `08ddb45`. **Items 1, 3, 4, 5 are OTA-shippable on their own** (JS/TS only) but are bundled into this native batch anyway since item 2 forces one: (1) deferred deep-link handoff for challenge invites — new `web/join.html` (`~/pick-app`) + `pendingChallenge.ts`; no Universal Links (needs a new native entitlement), landed on a clipboard-marker fallback instead, a known-imperfect but cheap approach — also removed the old `/join → /download` redirect in `web/_redirects`, worth checking before the next Netlify drop; (2) **Sign in with Apple** — `expo-apple-authentication` + `app.json` plugin + UI on login/signup — **the one item that actually requires this native build**, since the entitlement is written at prebuild time; (3) auto-join on invite arrival, gated on (1) landing first; (4) a one-line explainer before the location OS prompt, gated so it only shows once per install; (5) an auth guard on `challenge/[id].tsx` so an unauthenticated deep-link visitor is routed to signup with the challenge preserved instead of hitting a silent no-op join. Same commit also restored `MIN_CLEANUP_SECONDS` to 60 (see Launch gates) and removed a stray deleted screenshot from the working tree. `tsc --noEmit` clean, existing `npm test` suites unaffected. |
| Battery-drain investigation ("terrible drain after switching Location to Always") | 2026-08-31 | — (diagnosis only, not closed) | Real field report, not hypothetical — flagged here rather than under Closed-as-fixed since nothing was changed. Traced through `backgroundSession.ts`, `useAppInitialization.ts`, `crashRecorder.ts`, and the `stopCleanup`/`finishCleanup` path. The "Always" prompt itself is expected (`afaed31`, screen no longer force-stays-on). Normal walk-end paths (Stop & save, watch End, the Settings force-stop escape hatch) all reliably call `stopBackgroundSession()`. **The real, known, already-partially-mitigated gap:** a crash or uncontrolled JS-thread death mid-walk skips that call entirely. `crashRecorder.ts` (`4026ae3`) already added a black-box sentinel + cleanup for this, but the cleanup only runs on the *next cold launch*, gated on a >3min-stale heartbeat — between a crash and reopening the app, iOS's own `startLocationUpdatesAsync` registration (continuous, `Accuracy.High`, `pausesUpdatesAutomatically: false`) keeps running independent of the dead JS thread. This exactly matches `QA_TEST_PLAN.md`'s still-unchecked P0 "Phantom tracker check" and P1 "Force-stop background tracking" items — never confirmed on-device. Separately, `Accuracy.High` with auto-pause disabled is a deliberate, documented tradeoff (Balanced mis-mapped pickups across the street) and a real, expected battery cost of an *active* walk, not a leak. Net: very likely the mechanism Jake saw, if a recent walk ended other than via a clean Stop and the app wasn't reopened right after — not present on every walk. No fix written; the QA_TEST_PLAN P0 item is the natural place to close the loop. |
| Apple Developer Program License Agreement — Attachment 14 | 2026-08-18 | 2026-08-23 | Accepted by Jake in App Store Connect. Releases no longer blocked by an unsigned agreement. **Signing detail added 2026-08-24: signed 2026-08-23 16:45 UTC, team `H77DJ6QPJC`. Was a hard blocker for 5 days.** Evidence: Gmail message `1a02f83b25bbfe64`. |
| “EAS build limit reached / Build 32 cannot be cut” | 2026-08-13 | 2026-08-23 | **Closed as misread, not as resolved.** The 13 Aug email body says 80% of the Free-plan iOS limit used. Build 32 was never quota-blocked. Live quota row moved to Build 32 above. **Note 2026-08-24:** the 8/24 briefing described the ledger as having carried this as a hard blocker for 11 days. It did not — this ledger opened 8/23 and reclassified it the same day, 10 days after the email. The genuinely new information on 8/24 is the hard count: **13 of 15 used, 2 left** — now in Build budget. |
| Keep-awake while `sessionMode` is null | 2026-08-19 | 2026-08-24 | Decided by Jake — screen off during walks, battery is the constraint. Shipped OTA (`afaed31`). Resolved once it was reframed: the bug it was blamed for already had a direct fix in the field, and `sessionMode` had never been recorded, so it was a missing measurement rather than a judgment call. Full reasoning on the Launch-gates row. |
| Detector: pace-gate dead zone at 1.0–1.3 m/s | 2026-08-24 | 2026-08-24 | Zero-pick walks showed false positives rising as pace fell (1.34 m/s: 0.16/min, 1.19: 1.11, 1.07: 1.33). The absolute gate only fires above 1.3 and the relative gate switched off at 1.0, so ordinary moderate walking pace was covered by neither. Ceiling removed (`1d671fc`). Costs ~40% recall for picking without stopping (measured on C6a) — acceptable only while walk→STOP→pick is the normal technique; the code says to restore the ceiling if that premise changes. |
| Detector: trailing median measured stops, not pace | 2026-08-24 | 2026-08-24 | `trailingMedianSpeed` filtered on `speed > 0`, so readings taken *while stopped* counted as pace. On B6 that dragged the median to ~0.5 and set the gate's bar at 0.40 m/s — below a real stop. The gate disarmed itself in proportion to how often the user stopped to pick, i.e. it failed worst at exactly its intended job. Fixed in `fd05a95`. |
| Walk 1b: silent total sensor failure | 2026-08-24 | 2026-08-24 | A normal-looking 6-minute walk saved with a completely empty `motion_log`. `startListening()` ran location setup between the events reset and the accelerometer subscribe inside one `try`; `watchPositionAsync` rejected, the catch swallowed it, and the accelerometer never attached — while the route still drew from map.tsx's own watch, so nothing looked wrong. Sensors now attach first, location is best-effort in its own try, and `sensorsAttached()` raises an alert instead of letting someone walk for nothing. `1d671fc`. |
| CARTO basemap tiles required an API key | 2026-08-31 (inbox) | 2026-08-31 | CARTO added a fair-use gate to the `light_all` raster tile endpoint; requests without a key showed an "API KEY REQUIRED" watermark. Fixed in all 4 app call sites plus both website copies (`~/pick-app/web/map.html`, `web/city.html`). Reported via `LEDGER_INBOX.md` as shipped-but-uncommitted; **now committed as `8800f68`.** **Correction folded in same run: the first OTA publish (update group `e4de5103…`) actually shipped `?key=undefined` on all 4 URLs** — a stale Metro transform cache serving a cached `undefined` from before the env var existed. Jake confirmed the watermark persisted through two force-quit/reopen cycles. Re-published after `rm -rf .expo/metro-cache` (update group `64c042f5…`), this time verified by grepping the compiled bundle for the real key string, not just a clean CLI exit — that's now the standing lesson for any OTA touching an `EXPO_PUBLIC_` var. Status ladder: fixed + shipped (OTA, corrected + website deploy), field verify inside a running app instance still open. **Unrelated finding surfaced during that publish, not yet triaged:** the build log printed `RN persistence unavailable (wrong firebase bundle?) — auth will NOT survive app restarts`, consistently on the web-bundle build step only — likely web-target noise (`metro.config.js` exists to fix this for iOS/Android) but not independently confirmed on a real device. Worth a qa look. |
| Challenge Recap discovered to be fully built, not a pending spec | 2026-08-31 (inbox) | — (discovery, not a fix) | `docs/CHALLENGE_RECAP_SPEC.md` (drafted 3 Aug) is live in code — confirmed by reading it directly, not the docs. Shipped in `e5436c6`/`6e4f164`: `app/challenge/[id].tsx` shows "Share recap" on a completed challenge, `GroupRecapModal`/`GroupRecapCard.tsx`/`challengeRecap.ts` all match the spec. Two things shipped beyond spec'd v1 scope: a real Leaflet/CARTO street map on `AreaPreview` (spec text still says "SVG shape"), and Community cross-posting (`createChallengeRecapPost`) that the spec's own §6/9 deferred to v1.1. **None of this was previously mentioned anywhere in this ledger, `LAUNCH_BUGLIST.md`, or `PROJECT_TIMELINE.md`** — zero prior grep hits. **Not yet field-verified**: spec Phase 4 (a real 3+-person challenge run to completion) appears to have never happened — nothing in `docs/fielddata/` is a multi-person walk. `neighborhood`/`anywhere` challenges still get the un-upgraded plain-trophy empty state (no map) — likely the common case. This is the v1 that `CHALLENGE_RECAP_SPEC.md §11` (see In-progress table above) now proposes a v2 redesign on top of. |
| Both-sides-cleaned bug: route points crediting the wrong side of the street | 2026-08-31 | 2026-08-31 | `markRouteCleaned()`/`getCoverage()` tested each candidate segment independently against a flat 11m snap distance, so a real (non-synthetic) OSM sidewalk pair close enough together let a route point on one side also credit the opposite side. Fixed by bucketing each route point to its nearest segment first. Committed `b6a0e57`, new regression test added. Also brought the live in-walk map recolor effect onto the same constants the persisted-coverage path uses (was 15m/0.8 hardcoded separately) so what's shown while walking now matches what gets saved. |
| `team_stats` never wrote `total_bags` | 2026-08-31 | 2026-08-31 | `rebuildTeam()` wrote `total_weight` but not `total_bags`, so `getTeamsWithStats()` silently fell back to a client-side estimate instead of a real count, unlike the per-city public dashboard which already computes real bags via `bagsFor()`. Fixed to reuse that helper. Committed `3f6661d`; already deployed (`onCleanupWrite`, `rebuildTeamStats`) and backfilled for all 3 existing teams, verified directly against Firestore. Found while grounding the new Civic-Org Dashboard spec (see below) against real data. |
| Sentry crash reporting enabled and verified end-to-end | 2026-09-01 | 2026-09-01 | Ran the official setup wizard but reviewed its output before committing: removed a duplicate `Sentry.init()` from `app/_layout.tsx` (consolidated into existing `errorMonitoring.ts`), turned off `sendDefaultPii` (would ship IP address, conflicting with the same-day privacy policy update), left Session Replay and the feedback widget disabled (redundant with the app's own Settings → Send Feedback). DSN as `EXPO_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN` registered sensitive with EAS. Shipped OTA (`@sentry/react-native` was already compiled in since `fac45bb`). Committed `90c4c61` (`~/pick-app`), `0010c26` + `810e308` (here). **Confirmed working**: a temporary one-time verification event (`bf8e45e`) showed up in the Sentry Issues dashboard on a real TestFlight force-quit/reopen, then removed (`3d23ffa`). |
| Sign in with Apple — full saga, nonce fix + Firebase iOS-app registration | 2026-08-31 | 2026-09-01 | See the Build 32 section above for the complete sequence (nonce fix `fac45bb`, still failing after that fix, root-caused to zero iOS apps registered in the Firebase project, fixed by registering one — no code change). **CONFIRMED WORKING on-device 2026-09-01.** Diagnostic groundwork (`e343230`, `2dc69c6` — surface Firebase's real error message/code instead of a generic one) is what made the actual root cause visible in the first place. |
| Walk-recovery double-prompt / silently-broken Restore — four rounds to the real root cause | 2026-09-01 | 2026-09-01 | Reported as a double "Recover your last walk?" prompt after a force-quit mid-walk. Round 1 (`4a77e90`): added `isBackgroundLocationTaskRunning()` as ground truth alongside the heartbeat check — didn't fix the symptom. Round 2 (`5e8048b`): found the real mount effect was firing `Alert.alert()` more than once per launch; added a module-scoped once-per-launch guard — fixed the double-prompt but surfaced a worse bug: tapping Restore did nothing. Round 3 (`9b6c097`, the actual root cause): the whole prompt lived inside the Map screen's own mount effect, which genuinely remounts during launch, so Restore's `onPress` closure could capture a since-unmounted instance's now-silent `setState`. Moved the prompt to `app/_layout.tsx`'s root layout (mounts exactly once) with a small pub/sub (`sessionRecovery.ts`) so whichever Map instance is live when the user answers applies the draft. A separate, smaller round (`24ba580`) fixed a related first-tap-swallowed bug: the alert fired before the launch transition settled, now gated on `InteractionManager.runAfterInteractions()`. **CONFIRMED WORKING end-to-end by Jake** — force-quit, relaunch, Restore correctly opens the summary card. All four rounds shipped OTA, same lineage. |
| Legal-doc reconciliation across all five copies, plus a Sentinel-found last-updated-date bug | 2026-09-01 | 2026-09-01 | Camera/photo + Apple Health disclosure, Sentry disclosure, legal entity name (John Larkin Verbiest), effective-date bump, account-deletion copy fix, contact-email unification, feedback-data disclosure — all with Jake's explicit sign-off, applied across `web/privacy.html`, `web/terms.html`, `apps/companion/src/constants/legal.ts`, and the two now-superseded doc stubs. Committed `0010c26` (here) + a matching commit in `~/pick-app`. **One real bug found in the same pass and fixed** (`ab023df`): `legal.ts` shared a single `LEGAL_LAST_UPDATED` constant between Privacy and Terms, so the privacy-only update silently back-dated the in-app Terms of Service too. Split into `PRIVACY_LAST_UPDATED`/`TERMS_LAST_UPDATED`. Full detail lives in `~/pick-app/docs/OPS_STATUS.md`'s Safety section, not duplicated here. |
| CARTO watermark regression #3 (`publish-detector.sh` run without sourcing `.env`) | 2026-09-01 | 2026-09-01 | Third occurrence of the same failure class (env var only reaches a local `eas update` bundle if sourced into the shell first) — this time triggered by a local publish, not a cloud `eas build`. Fixed by re-running with `.env` exported first; verified the real key landed in the shipped bundle via `grep -c`. **Confirmed by Jake** on the same update that also carried the walk-recovery timing fix. `publish-detector.sh` still needs hardening to source `.env` automatically rather than relying on the operator remembering — flagged, not yet done. |
| Overpass reliability — three real bugs found and fixed via live field testing during an actual mirror outage | 2026-09-01 | 2026-09-01 | (1) `26619ac` de-duped concurrent Overpass fetches across overlapping callers (pan handler, level-exit refresh, post-save refresh). (2) `2234d40` replaced sequential 3-mirror fallback (up to 45s worst case) with a 6s hedge — next mirror starts concurrently if the first hasn't answered, first response wins. (3) `bcb2d91` fixed a false "0% · complete!" shown on a **total** Overpass outage (confirmed live via curl: 2 of 3 mirrors down) — added `getCoverageOrThrow()` so a genuine total failure surfaces a real "Couldn't load, try again" banner instead of a fabricated complete state. (4) `1ca79ec` closed the gap in (3): a **majority**-fail/minority-succeed tiled result (e.g. Sunset Park settling on an implausible "36 to go") was sailing through as a real count — now counts real successes/failures per tile and refuses below 50% success rate. **All four fixes verified against real, live, adversarial conditions** — a genuine partial outage, then a genuine total outage across all three public mirrors, both independently reconfirmed via direct curl during the session, not assumed. See the new Launch-gates row above for the structural risk this surfaced (Jake's explicit priority for 2026-09-02). |
| Cloud Functions cost fix — incremental Firestore rollups replacing three full collection scans | 2026-09-01 | 2026-09-01 | Routed from `finance`'s cost-growth analysis (a full `cleanups` scan on every hourly cron tick, every `org.html` pageview, and every single cleanup write — cost scales with cumulative history, not concurrent users, and was projected to approach Firestore's free-tier ceiling in weeks at current tester pace). `rebuildTeam`/`onCleanupWrite` now applies a per-cleanup delta inside a transaction instead of rescanning; `orgDashboard` now reads a maintained `org_stats/{teamId}` rollup instead of scanning live; `scheduledOrgSnapshots`'s existing weekly full recompute now also serves as the correctness backstop. `scheduledPublicStats` (global/city aggregates) was deliberately left as a full scan — needs real new infrastructure for its rolling-window/decay requirements — but its cron cut 6x, hourly → every 4 hours, for a proportional cost cut with zero drift risk. **Deployed live via `firebase deploy`, then committed** (`5400ed7`) after a process gap where the deploy had run ahead of the commit — repo now matches production. |
| Website copy trim + stale detector claim — deployed to pickglobal.org | 2026-09-01 | 2026-09-01 | Closes two items that had sat undeployed/unfixed: the Aug 23 word-count trim (1,168 → 920 words, Option A) and the stale claim on `index`/`about`/`support` that the detector still over-counts on a slow stroll (untrue since the 19 Aug fix). Committed `2c19344` (`~/pick-app`); Jake confirmed the Netlify manual drop is live. Netlify drops the whole `web/` folder, so `web/org.html` (the civic-org sponsor dashboard) went out in this same drop — confirmed live via a direct browser check at the time (correctly showed "No token provided" for a tokenless request). |
| **Overpass pre-cache fully implemented and deployed** — spec → live in one session | 2026-09-03 | 2026-09-03 | Full detail on the Launch-gates row above (now struck through as resolved). Summary: built the shared fetch/hedge/chop pipeline, two new Cloud Functions, two new Firestore collections, and client cache-first checks; found and fixed a real Firestore nested-array rejection on write; shipped two OTA publishes same day (the second correcting the first, which had gone out before the client-side unflatten fix existed); broadened the seed list, raised the function timeout to 1800s, added a same-run retry. Confirmed live via a direct Firestore REST read of a correctly-shaped Fort Greene precache doc. |
| **Real bug: `orgDashboard` missing CORS header blocked every sponsor-dashboard load since it shipped** | 2026-09-03 | 2026-09-03 | `functions/index.js`'s `orgDashboard` never sent `Access-Control-Allow-Origin`, so `web/org.html`'s client-side `fetch()` was silently blocked by the browser on every load since the Aug 31 ship — invisible to the endpoint's own request-handling tests (CORS is browser-enforced) and to the earlier "no token provided" check (that path never reaches the fetch). Found live-testing the first real sponsor onboarding end-to-end (**Litter Legion, Astoria, NY** — the first real sponsor team created, via `create-sponsor-team.js`). Fixed with `res.set('Access-Control-Allow-Origin', '*')` (the endpoint is already public/token-gated, so `*` matches its intended access model), committed **`1cdc969`**, redeployed via `firebase deploy --only functions:orgDashboard`. Re-verified live: `pickglobal.org/org.html?token=...` now renders "Litter Legion — PICK district impact" with correct (zero — no Astoria cleanups logged yet) district totals. CRM now has a real, working dashboard link for outreach. |
| `team_stats` incremental rollup smoke-tested against real data; one drift found and fixed | 2026-09-03 | 2026-09-03 | Verified the 2026-09-01 incremental-rollup rewrite (see that Closed row) against a real logged cleanup under team "FED U.P.": `total_cleanups`/`total_pickups` matched an independent live full-scan exactly (64/1903 both sides), once cross-checked against the correct field (`items_count`, the user-confirmed total, not the raw pre-confirmation `items_detected`). **One real but minor drift found and fixed**: `member_count` was stuck at 2 while the `members` refcount subcollection (source of truth) had only 1 doc — likely stale from before the incremental-rollup rewrite, not caused by the smoke test. Corrected directly (2 → 1) via a one-off admin script, since deleted. Not checked on other teams — worth a quick pass on `team_stats/"CG Pickers"` and `team_stats/"Slim Pickins"` if this class of bug matters enough to chase. |
| Group Recap v2 (Tier 1) — built, then deployed on Jake's go-ahead | 2026-09-04 | 2026-09-04 | `CHALLENGE_RECAP_SPEC.md` §11 Tier 1 scope only (Tier 2 private-photo compositing explicitly not built, a separate future privacy call). Committed `9d4856f`: `createPost` gained optional `challengeId`, new `getPostsForChallenge()` (new `posts` composite index), `challengeNeighborhoodBoundary()` added to `neighborhoods.ts` for name-only `area.type === 'neighborhood'` challenges, `GroupRecapCard` redesigned (map promoted from a 150px strip to a 220px centerpiece, stat tiles below not overlaid, new photo strip, improved 'anywhere' placeholder). One real design fix made during the build, not after: `map.tsx`'s `liveEvent` state is not a reliable "active challenge" signal at share time (clears the moment a walk stops, scoped to `pickups`-goal challenges only) — both `createPost` call sites now do a fresh `findMyActiveChallenge()` lookup instead. `tsc --noEmit` clean; existing suites pass except the pre-existing, unrelated `test:hoods` transform failure. **Deployed same day on Jake's explicit go-ahead**: `firebase deploy --only firestore:indexes` (new `posts` index, live) + `eas update --branch production` (group `81b35fbf-9a94-4917-a8b1-07c878370e3d`, runtime 1.2.2). CARTO key verified correctly inlined in the shipped bundle. No native change, EAS quota untouched. **Not yet field-tested** against a real challenge with tagged photos and a neighborhood-type area (spec §11.5 phase 5) — worth doing before calling this fully verified. |
| IDEAS_INBOX: bundle-size bloat risk investigated and cleared; neighborhood-loading slowness root-caused | 2026-09-04 | 2026-09-04 (investigation only, no code changed) | Two `docs/IDEAS_INBOX.md` (`~/pick-app`) backlog items looked into. **Bundle-size bloat: not a real risk, confirmed with real numbers.** `CITY_SOURCES` stores only URLs/bbox config per city — actual GeoJSON is fetched at runtime to on-device `FileSystem`, never bundled. A real `expo export` (2026-09-04) shows `neighborhoods.ts`+`streetSegments.ts` at ~73KB of a 10.93MB source total, under 0.7%, dwarfed by Firebase (~1.6MB)/Sentry (~430KB) in the same bundle — recommend closing this backlog item. **Neighborhood loading: still real and current, and the root cause had moved since build 28.** Traced the exact call graph and confirmed `activateHood()` → `getSegmentsForRing()` → `fetchStreetGeometryForRing()` never queried `precache_streets` — only the point-radius and boundary-fallback paths did — so even Fort Greene (already seeded and confirmed live in `precache_streets`) got zero benefit from the Sept 2-3 precache when a real user tapped into it. This is the finding that directly produced the ring-precache fix below (implemented and shipped the same day this was found). |
| Ring-precache gap fix — implemented, then published OTA | 2026-09-04 | 2026-09-04 | Directly closes the root cause found above. `streetSegments.ts`'s `getSegmentsForRing` now checks a new `getPrecachedSegmentsForRing()` (require ALL 0.01° grid cells covering the ring's bbox to be a precache hit, any single miss falls through to today's unchanged live-Overpass path — rejected a partial-merge alternative since it wouldn't save real latency and would reintroduce the per-cell tiling build 28/29 deliberately moved away from) before falling through to live Overpass. Cost-checked against Finance's growth-cost-ceiling model: billed via a chunked `'in'` query (1 read on a full miss, not N), gated behind the existing 30-day on-device ring cache so repeat activations are free — projected at most a few thousand extra reads/week, trivial against the 50K/day free tier. `tsc --noEmit` clean; `test:geometry` (14/14) and `test:tiles` (12/12) pass; `test:hoods` fails identically to the pre-existing, unrelated transform error. **Published OTA same day on Jake's go-ahead** — update group `ae49781d-20f1-47b1-99d3-5143b9821f12`, runtime 1.2.2, both platforms, from `production` (confirmed as the live Build 36 branch via `eas channel:list`/`eas update:list` before publishing, not assumed). Still uncommitted in git as of this run — see "In progress, uncommitted" above. **Not yet field-tested** against a real cold-cache activation (e.g. Fort Greene) to confirm a precache hit actually resolves fast rather than hitting the 22s "Still loading" banner. |
| 8-city curation batch — implemented, then published OTA | 2026-09-05 | 2026-09-05 | All 8 cities flagged by `~/pick-app/docs/GLOBAL_CITY_EXPANSION_TARGETS.md`'s map-coverage validation pass (Seattle, LA, Chicago, Boston, San Diego, Miami, Amsterdam, Bristol) given a real bespoke `CITY_SOURCES` entry in `neighborhoods.ts`, each sourced from the city's own official GIS/open-data layer (never a generic OSM widen where the validation pass had already flagged that tier as sparse/unsystematic, e.g. Seattle), each confirmed by point-in-polygon spot checks, and confirmed to have no bbox overlap with any existing or new entry. **One real bug found and fixed in the same pass**: Amsterdam's `data.amsterdam.nl` API 406s on this file's existing bare `Accept: 'application/json'` header — would have silently produced permanent empty hoods for Amsterdam with no visible error. Fixed by widening the header to `'application/geo+json, application/json'`, confirmed not to break any existing city. `tsc --noEmit` clean. **Published OTA on Jake's explicit "ship it"** — update group `2907d543-ece9-4159-afa5-7632a16de475`, runtime 1.2.2, both platforms, re-verified independently before publishing (diff spot-checked, not just trusted from the write-up) rather than assumed clean. Still uncommitted in git — see "In progress, uncommitted" above. `basemapLabels` defaulted `false` for all 8 (Atlanta/SF precedent) but not independently re-confirmed against real rendered tiles. **Not yet field-tested** on a real device against any of the 8 new cities. |
| Overpass client reconciled against real OSM wiki fair-use text — one live production gap fixed | 2026-09-05 | 2026-09-05 (part of the still-undeployed NYC-wide drip design, but this specific fix touches already-live code) | While designing the NYC-wide precache drip (see the new flagged Launch-gates row), a session checked `overpassClient.js` against the OSM wiki's actual fair-use guidance rather than assuming today's code already complied. Found a real gap that is **currently live in production, not just in the undeployed design**: a 429/406 response triggered an immediate hedge-fallback or next-tile retry with no pause at all — the wiki's literal "pause 30 seconds before a new request" was never implemented, and `overpassClient.js` has been live since `cd0f4b0` (2026-09-03), so the weekly `scheduledOverpassPrecacheRefresh` has been running with this gap since then. Fixed with an opt-in (not global-default, to avoid regressing already-tuned client map-load UX) `enforceCooldown` flag, wired only into the two Cloud Functions precache call sites. Also added Overpass-client contact info to the User-Agent string per the wiki's guidance. **Still staged, uncommitted, not deployed** alongside the rest of the NYC-wide drip work — this fix does not reach production until that deploy happens. |

## Testers & field data

- **TestFlight:** **Build 36 is current as of 2026-09-03** (Jake-confirmed) — carries the full Build 32 batch plus Sign in with Apple. Build 31 (`1.2.2`) had been live since 2026-08-13; builds 27–31 all cleared 12–13 Aug.
- **Detector validation:** B5 result was 20 counted / 20 real, 1.00x — **validated on one tester.**
- **Next milestone:** multi-tester detector validation. `pace_median_mps` now saves on
  every walk, so tester walks self-label by pace.
- Field logs in `docs/fielddata/`: A7a, C6a, C7a, B4, B5B, B5.

## 📨 Notification dedupe seed

Carried over from `pick-state.json` (Drive) when it was retired on 2026-08-24. The
scheduled run has no other memory of what it has already reported, so these Gmail
message ids were already surfaced to Jake and must not be re-notified:

`1a015d04ff471bff` · `19ffbfce53fed803` · `19ffc305705336fc` · `1a02f83b25bbfe64` (the DPLA acceptance)

Gmail was scanned through **2026-08-24T00:00:00Z**. Append ids here as runs notify on them.

> **Drive memory retired 2026-08-24.** `pick-state.json` and
> `pick-state.NEXT-2026-08-24b.json` were the cloud run's memory, needed only because a
> cloud session could not reach this Mac. The scheduled task is now device-bound, so this
> ledger is the single record. Both files are in Drive's trash (recoverable ~30 days) —
> everything in them that was still live was moved here first. A third,
> `pick-state.NEXT-2026-08-24.json`, had already been trashed.

## Agent run log

| 2026-09-05 (Claude Code) | Largest inbox fold-in since 2026-09-01 — spanning 2026-09-03 through 2026-09-05 entries. One new commit (`9d4856f`, Group Recap v2) and three OTA publishes since the last pass (Group Recap v2, the ring-precache fix, the 8-city curation batch) folded into new Closed rows. An IDEAS_INBOX investigation folded in: bundle-size bloat cleared as a non-issue, neighborhood-loading slowness root-caused to the ring-fetch path never consulting `precache_streets` (which directly produced the ring-precache fix). Website table's stale "1,168 words" row corrected — it disagreed with the ledger's own Closed section, which has said 920 words (deployed) since Sep 1; also added the Sep 3 litter-hotspots-removal finding that hadn't been recorded anywhere in this table. **New flagged Launch-gates row added, the most significant item this run**: a large NYC-wide Overpass precache drip (1,225 tiles, roster+drip Cloud Function design) is fully designed, reconciled against real OSM fair-use text (including a cooldown fix for a gap that's been live in production since Sep 3), and staged — but explicitly NOT deployed. A same-day deploy request relayed through another agent, claiming Jake had approved it in a conversation this session couldn't see, was correctly declined by the session that received it; flagged here so the decision doesn't get relitigated on secondhand authority in a future run — it needs to reach an interactive session directly. "In progress, uncommitted" rewritten: six app-code files are modified but uncommitted, two of which (the ring-precache fix, the 8-city batch) are already OTA-live despite that, one group (the precache-drip files) is genuinely undeployed. Two new untracked non-app items flagged (`PICK_seed_shotlist.txt`, `docs/TEST_DATA_WIPE_PLAN.md`), alongside the two already-flagged from last run. Repo hygiene: unpushed-commits row updated 35→36. New Watching row added for the three unverified OTA publishes. Did not attempt `eas build:list` (blocked by this session's own auto-mode classifier as a network action), `git push`, `git commit`, or a Gmail check, per the job's read-only mandate. Part 2: `~/pick-app/docs/OPS_STATUS.md`'s Product-section staleness banner on `PROJECT_TIMELINE.md` corrected — the timeline actually gained a new Sep 4 entry (Android timing decision) since the banner was last written, so "newest entry is Aug 31, 3 days stale" was itself stale; rewritten to note the real newest entry and what's still missing from it (the Sep 3 Overpass full-deployment/CORS-fix news, and all of this run's Sep 4-5 ships). No other material drift found between OPS_STATUS.md and what this pass confirmed — OPS_STATUS was already unusually current, having been updated same-day by Finance (Overpass drip budget/log verification) and Safety (GDPR review) passes that ran ahead of this one. |
| 2026-09-03 (Claude Code, second same-day follow-up) | Overpass pre-cache went from "spec written, 6 open questions" to fully implemented and deployed — 7 new commits since the prior pass (`cd0f4b0`…`ef1a9bc`, `1cdc969`), all folded in. Launch-gates row struck through and rewritten; two new Closed rows added (pre-cache implementation, and a real CORS bug on `orgDashboard` that had silently blocked every sponsor-dashboard load since Aug 31 — found live-testing the first real sponsor, Litter Legion/Astoria, fixed and redeployed same day). Closed the stale "still open: needs Netlify drop" caveat on the existing civic-org-dashboard row — it went live in the Sep 1 drop, just non-functional until today's CORS fix. Added a third new Closed row for a `team_stats` smoke test that confirmed the incremental rollup accurate and fixed one stale `member_count`. Updated the Long-walk-crash launch-gates row with a first, reassuring-but-not-conclusive 1-hour field-test result. Repo hygiene: unpushed-commits row corrected 28→35 ahead (today's commits); "In progress, uncommitted" corrected — app code is clean again, and flagged two out-of-place untracked non-app files (`PICK Content/`, `youtube_banner_v4_2560x1440.png`) found in the working tree, left untouched. `LEDGER_INBOX.md` cleared back to template. Part 2: `~/pick-app/docs/OPS_STATUS.md` corrected in several places that had gone stale relative to what this pass confirmed — see its own "Last updated" line for the list. Did not attempt `eas build:list`, `git push`, `git commit`, or a Gmail check, per the job's read-only mandate. |
| 2026-09-03 (Claude Code, same-day follow-up) | Jake directly confirmed in chat: **the current TestFlight build is 36.** Closes out the open "35 vs 36, strongly implied but not independently confirmed" question from earlier this same run — updated the Build 32 section (renamed header to "confirmed live as TestFlight Build 36"), the build-naming-convention note, the Public-beta table's Approved-build row, and the Testers & field data TestFlight line, all of which still said Build 31 or left the number open. "Build 32" is now retired as a working name — TestFlight Build 36 is the real, Apple-confirmed number, carrying the full batch plus Sign in with Apple. |
| 2026-09-03 (Claude Code) | No new commits since 2026-09-01 (all of 09-02/09-03 was research/spec work, correctly uncommitted) — this run folded a two-day backlog of `LEDGER_INBOX.md` entries into the ledger's actual structure instead. Build 32: added the 09-02 finding that EAS build 35/36's `appBuildVersion` (35, 36) strongly implies those are the real TestFlight numbers, though App Store Connect itself still isn't independently checked. Launch gates: rewrote the Overpass structural-risk row from "needs a decision" to "decided" — Jake picked Option C (server-side pre-cache) after all three options were scoped, `docs/OVERPASS_PRECACHE_SPEC.md` is written with 6 open questions left for Jake, and three straight days of mirror-flapping data plus 09-03's vendor-pricing research (no self-serve pricing for a paid host; self-host sizing sourced but still makes Jake the uptime owner) reinforce rather than change that call. Five new Closed rows: the Overpass options/decision/spec, a neighborhood-architecture review (SF fully committed — corrects `~/pick-app`'s stale exploration-doc header; Atlanta's live ArcGIS sourcing flagged as a fourth pattern; LA/Chicago's neighborhood data confirmed 100% point-only via live Overpass), the ZIP/postal boundary-tier spec (Census ZCTA source verified live, a real "no current-year file, only the 2020 vintage" gotcha closed), the TestFlight-number investigation, and the 09-03 mirror/vendor-pricing follow-up. Repo hygiene: unpushed-commits row reconfirmed unchanged at 28 ahead (now 8 days open). "In progress, uncommitted" updated to note the three new untracked spec docs (no app code touched). `LEDGER_INBOX.md` cleared back to template. Part 2: `~/pick-app/docs/OPS_STATUS.md`'s Product-section staleness flag on `PROJECT_TIMELINE.md` updated — that file's newest entry is still Aug 31, now 3 days stale (was 1), missing Sign in with Apple's full resolution plus the Overpass Option C decision; corrected in OPS_STATUS.md directly per its own "safe to update in place" rule, not written into the append-only timeline. Did not attempt `eas build:list` (beyond confirming the CLI is reachable), `git push`, `git commit`, or a Gmail check, per the job's read-only mandate. |
| 2026-09-01 (Claude Code) | Largest single-run reconciliation to date — 17 new commits folded in from a full day's inbox trail. Build 32 section rewritten: EAS build 35 confirmed submitted 2026-08-31 (real TestFlight number still unconfirmed), Sign in with Apple's full multi-round saga closed out end-to-end (nonce fix → still failing → root-caused to a missing Firebase iOS-app registration → confirmed working on-device), EAS-internal build 36 cut 2026-09-01 on quota reset. Build budget corrected: quota reset 2026-09-01, 1/15 used (not independently re-verified against the live billing page this run). Added a new Launch-gates row for Overpass mirror reliance as a structural risk, per Jake's own explicit "major priority for 2026-09-02" flag — grounded in three real bugs found and fixed via live field testing during an actual multi-mirror outage (sequential-retry → hedge; false-complete-on-total-outage; false-complete-on-partial-outage). Seven new Closed rows added: Sentry (shipped + verified), Sign in with Apple, walk-recovery double-prompt (four rounds to the real root cause — Map-screen remount stale closures), legal-doc reconciliation (cross-referencing `~/pick-app/docs/OPS_STATUS.md` for full detail), CARTO regression #3, the Overpass reliability batch, the Cloud Functions incremental-rollup cost fix (deployed via `firebase deploy`, then committed after a same-day process gap), and the website copy-trim deploy. Repo hygiene: unpushed-commits row updated 11→28 ahead, flagged as an aging gap open since 2026-08-26. "In progress, uncommitted" reconfirmed all-clear — only this job's own ledger/inbox files were modified in the working tree. `LEDGER_INBOX.md` cleared back to template. Part 2: read `~/pick-app/docs/OPS_STATUS.md` and `PROJECT_TIMELINE.md` — found them already well-reconciled against this ledger (an earlier session/pass had already corrected the EAS line and civic-org-dashboard status), so no material drift to fix in OPS_STATUS.md this run beyond what it already flags itself (`PROJECT_TIMELINE.md`'s newest entry is still 2026-08-31, now missing an entire day of 2026-09-01 work — OPS_STATUS already carries its own "known drift" note for this, left as-is per the append-only rule). Did not attempt `eas build:list`/`git push`/`git commit`/Gmail check per the job's read-only mandate — Build 35/36's real TestFlight number is genuinely unknown; check it next run. |
| 2026-08-31 (Claude Code, second pass today) | All 5 "in progress, uncommitted" items from the first pass folded into Closed as landed commits (city-requests `0224b04`; civic-org dashboard `7b33ce2`/`a95bb63`, incl. a real bug found+fixed via manual testing; Build 32 signup-flow batch `08ddb45`, incl. `MIN_CLEANUP_SECONDS` correctly landing at **60**, not the 120 this ledger and `PROJECT_TIMELINE.md` both assumed); Build 32 reframed from "assembled, waiting" to "third EAS attempt in flight" after two lockfile-drift failures (`d3549fb` fixed it); EAS quota section corrected to the confirmed 15/month cap, 14 used before the in-flight attempt, possibly the last slot this cycle; unpushed-commits row updated 6→11 ahead; a new battery-drain investigation logged (diagnosis only, ties to the still-unchecked `QA_TEST_PLAN.md` P0 phantom-tracker item); `LEDGER_INBOX.md` cleared back to template. Part 2: `OPS_STATUS.md`'s EAS-budget line and civic-org-dashboard status corrected (dashboard moved from "spec, not approved" to "built, deployed, bug-fixed" — only Challenge Recap v2 remains an unapproved spec); flagged that `PROJECT_TIMELINE.md`'s Aug 31 entry (written before this run's second-pass work happened) is now the same-day narrative gap, missing the fact that the civic-org spec it called "neither approved for building as written" was fully built within hours. Did not attempt `eas build:list`/`git push`/`git commit`/Gmail check per the job's read-only mandate — so Build 32's third-attempt outcome is genuinely unknown; check it next run. |
| 2026-08-31 (Claude Code, first pass) | CARTO inbox entry folded in and marked committed (`8800f68`); two new committed fixes recorded (`b6a0e57` coverage bug, `3f6661d` team_stats bug); unpushed-commits row updated 3→6 ahead; new "In progress, uncommitted" section added as a snapshot of a live session's in-flight work (city-requests feature, two new specs); `LEDGER_INBOX.md` cleared back to template. Part 2: `OPS_STATUS.md` corrected — its stale-timeline banner on `PROJECT_TIMELINE.md` was itself stale (that file has a real 2026-08-31 entry; not stale since Aug 18), and its "Last updated" date bumped. **Five-day gap since the last run (2026-08-26 → 2026-08-31) — this task did not fire daily as designed; worth checking why with Jake** (see summary). Did not attempt `eas build`/`git push`/`git commit`/Gmail check per the job's read-only mandate; the working tree was changing between reads in this same run (a live session appears to be active), so the "in progress" table above is a snapshot, not a guarantee. |
| 2026-08-26 (Claude Code, manual re-run) | Nothing — clean pass | Re-ran on request, second local pass today. `HEAD` unchanged at `32e1f90` since the prior pass this morning (no new commits, `main` still 3 ahead of `origin/main`), no new files in `docs/fielddata/`, `LAUNCH_BUGLIST.md`/`SHIPPING_PLAN.md` mtimes unchanged. Checked the new `docs/LEDGER_INBOX.md` per the single-writer process that was set up between the two runs — empty, nothing to fold. Also traced down the routine that sent this morning's "ledger did not update" push notification: it's the cloud-hosted `trig_01RvnozDsHJLd5wkQ9jHxFvG` ("Pick — launch ledger refresh"), a second, separately-created cloud routine that fails the same way the original `trig_01AkTNjzshGa6MTbXifjGQvD` ("Pick — Chief of Staff") does — confirmed via its run log, and confirmed the older routine is also still firing twice daily and has resumed writing a `pick-state.json` fallback to Drive, undoing the 2026-08-24 Drive-memory retirement above. Jake was told to delete both at claude.ai/code/routines; neither is deleted as of this run. |
| 2026-08-26 (Claude Code, parallel-verification run) | Repo hygiene: helper scripts confirmed tracked, unpushed-commits row reopened, stale index.lock cleared; detector section given the isNotStriding/session_mode mechanism it was missing | This is a Claude Code session running the same job the local Cowork task runs daily at 16:30 ET, in parallel for a verification period per the task brief — not a replacement yet. Working tree was clean; no uncommitted changes to reconcile. Read `docs/LAUNCH_BUGLIST.md` and `docs/SHIPPING_PLAN.md` for cross-check only, per house rule preferring the ledger/buglist over the shipping plan and newest-mtime-wins on conflicts — did not edit either (out of scope for this job). Did not check Gmail (not part of this job's steps, unlike the retired Drive-memory-era task) — the notification dedupe seed below is unchanged and unverified this run. Did not attempt `eas build`/`git push`/`git commit` per the job's read-only mandate. |
| 2026-08-24 (local, retire Drive memory) | Drive `pick-state*.json` trashed; live content migrated here | Ledger is now the sole record. Migrated before trashing: the outstanding test-walk results (which existed nowhere else), the DPLA evidence message id, and the notification dedupe seed. Dead on arrival and not migrated: the "3s timer redrawing a local cache" caveat (that path does not exist — the watch has no polling timer), the `watch-live-updates` item (already shipped in `WatchSessionModule.swift`), and `routine-goes-local` (done — task `trig_01RvnozDsHJLd5wkQ9jHxFvG`, daily 16:30 ET). |
| 2026-08-24 (local, watch changeset) | Flash mechanism identified; B + D written; `ship-watch.sh` added | Read the watch target end to end. **Jake's second suspect — a 3s timer redrawing a local cache — does not exist:** the watch has no polling timer at all (one 25s one-shot, one 6s disarm) and is already `@Published`-driven. The 3s lives on the *phone*, as a throttle on the clock tick that counts already bypass. **Item 3 (event-driven WatchConnectivity) is already shipped** — `updateApplicationContext` + `sendMessage` are both in `WatchSessionModule.swift`, `transferUserInfo` appears nowhere; dropped as no-op work. Real stale-render path found instead, and fixed by A+B. `tsc --noEmit` clean; lint shows no new findings. **Still no Swift toolchain — A/B/C remain uncompiled, which is now the only gate.** |
| 2026-08-24 (local, reconcile) | EAS → budget line with real counts; PhoneLink re-decided with first field evidence; two-build plan recorded; build naming settled | Cloud run 2026-08-24 could not reach this Mac; reconciled from Jake's briefing. Two briefing items were already true here — EAS reclassified 8/23, DPLA closed 8/23 — so the new detail was added rather than a second correction. One briefing item corrected: `PhoneLink.swift` is implemented in tree, not merely decided. `SHIPPING_PLAN.md` §1 flagged stale alongside §2. Repo hygiene: `main` now matches `origin/main`. |
| 2026-08-23 (local, 4th) | Public link confirmed live; docs committed | Beta is already public — reframes go/no-go as "tighten what's running". Site copy corrected on disk (4 pages) and index trimmed 1168→920 words; **not yet deployed**. Ledger + docs committed in `9c0b778`. **Push still pending — no GitHub credentials reachable from the agent session.** |
| 2026-08-23 (local, 3rd) | Public beta assessed; website scoped | Build 31 is already approved — a public beta needs no new build. LLC deferred by Jake. Two docs added. Open question: whether the TestFlight link is already public. |
| 2026-08-23 (local, 2nd) | PhoneLink resolved in tree | Jake chose the split-response option over commit-as-is / revert. Written, unverified, uncommitted. Opened a Watching section. |
| 2026-08-23 (local) | EAS blocker retired; PhoneLink row corrected | Re-read the 13 Aug Expo email: 80% used, a warning, not a stop — EAS moved out of hard blockers. PhoneLink row said the change "removes" the guard; the diff broadens it. No hard blockers open. `pick-state.json` in Drive still carries both stale facts — see note below. |
| 2026-08-23 | Apple DPLA accepted | Hard blocker closed by Jake. |
| 2026-08-23 seed | — | Ledger created. Four findings surfaced from Gmail + repo state; see hard blockers and repo hygiene. |
